{
  "name": "UserTaskProgress",
  "type": "object",
  "properties": {
    "day": {
      "type": "number",
      "minimum": 1,
      "maximum": 28,
      "description": "Program day 1-28"
    },
    "task_index": {
      "type": "number",
      "minimum": 0
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "complete",
        "skipped"
      ],
      "default": "pending"
    },
    "skipped_reason": {
      "type": "string",
      "maxLength": 500
    },
    "completed_date": {
      "type": "string",
      "format": "date"
    },
    "coach_id": {
      "type": "string",
      "maxLength": 64
    }
  },
  "required": [
    "day",
    "task_index"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    }
  }
}
