{
  "name": "Template",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "maxLength": 120
    },
    "category": {
      "type": "string",
      "maxLength": 60
    },
    "content": {
      "type": "string",
      "maxLength": 25000,
      "description": "Markdown. 25000 holds a long-form checklist; still bounded to prevent runaway payloads."
    },
    "variables": {
      "type": "string",
      "maxLength": 500,
      "description": "Comma-separated variable hints"
    }
  },
  "required": [
    "title",
    "category",
    "content"
  ],
  "rls": {
    "read": {},
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
