{
  "name": "ObjectionResponse",
  "type": "object",
  "properties": {
    "objection_key": {
      "type": "string",
      "maxLength": 60
    },
    "objection_text": {
      "type": "string",
      "maxLength": 500
    },
    "annette_answer": {
      "type": "string",
      "maxLength": 2000
    },
    "host_response": {
      "type": "string",
      "maxLength": 2000
    },
    "coach_id": {
      "type": "string"
    }
  },
  "required": [
    "objection_key"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "coach"
          }
        }
      ]
    }
  }
}