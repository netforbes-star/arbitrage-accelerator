import ResourceManager from "@/components/resources/ResourceManager";

/** Workspace tab — full resource management (published + unpublished). */
export default function ResourcesTab() {
  return <ResourceManager />;
}